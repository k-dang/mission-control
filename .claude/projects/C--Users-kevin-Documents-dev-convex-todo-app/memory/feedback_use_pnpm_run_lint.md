---
name: Use pnpm run lint
description: User prefers pnpm run lint over running eslint directly
type: feedback
---

Use `pnpm run lint` instead of `pnpm exec eslint` for linting.

**Why:** User explicitly corrected this preference.

**How to apply:** Always use `pnpm run lint` when validating code changes in this project.
